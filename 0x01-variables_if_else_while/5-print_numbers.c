#include <stdio.h>

/**
 * main - print numbers with single digit from 0 to 10
 *
 * Return: always 0
 */
int main(void)
{
	int n;

	for (n = '0'; n < '10'; n++)
		putchar(n);
		{
			putchar('\n');
		}
	return (0);
}
